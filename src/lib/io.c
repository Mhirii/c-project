#include "io.h"
#include "log.h"

FILE *open_file(char *filename, char *mode) {
  LOG_ERR("unimplemented");
  return NULL;
}

char *read_file(FILE *file) {
  LOG_ERR("unimplemented");
  return NULL;
};

void close_file(FILE *file) { LOG_ERR("unimplemented"); };

int check_path_exists(char *path) {
  LOG_ERR("unimplemented");
  return 0;
}

int check_file_exists(char *filename) {
  LOG_ERR("unimplemented");
  return 0;
}
